{
  "name": "@nfe/database",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "mongoose": "^8.3.4",
    "@nfe/config": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.5",
    "@nfe/tsconfig": "workspace:*"
  }
}
